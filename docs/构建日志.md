# 构建日志 —— 从 0 搭一个可成长的 Cordis CLI 模板

> **这份文档是给未来的自己(和 AI 助手)看的。**
> 它记录每一步做了什么、为什么这么做、以及下一步该干嘛。
> **上下文丢失后,从这里恢复。**

## 背景

- **学习材料**:`/Users/edwin/coding/aitool/aiinfra/study/cordis-study/`
  - `docs/` —— Cordis 原理系列 **17 篇 + 附录 A/B/C/D**(约 1.2 万行,全部实测验证)
  - `docs/17-review.md` 第三节是**按症状检索的查症手册**,遇到问题先查那里
  - `mini-dsh/` —— 一个已完成的麻雀项目(可运行,5 个文件的完整 seam)
- **参考项目**:`/Users/edwin/coding/aitool/deepseek-harness`(已 clone,227 包,node_modules 已装)
- **自有项目**:`/Users/edwin/coding/diaoyan/upliftlyproject/upliftly`(pnpm + Turborepo + NestJS)

## 协作方式(重要)

> **我(Edwin)动手,AI 引导。** AI 给完整代码 + 讲清为什么,我自己建文件、跑命令、贴结果。
> AI 不代劳建文件,但**可以代劳提交和写文档**。

---

## 成长路线(10 步)

```
阶段 A · 地基
  [x] 步骤 0   workspace 骨架 + 目录约定
  [x] 步骤 1   第一个能跑的 bin（还不碰 Cordis）
阶段 B · 接入内核
  [x] 步骤 2   最小 boot + 第一个插件
  [x] 步骤 2.1 cordis.yml 移入 apps/cli
  [x] 步骤 2.2 默认裸 node 运行
  [x] 步骤 3   第一条能力 seam（契约/提供方/消费方）
  [x] 步骤 3.1 依赖体系整改（catalog / peer / 门禁）
阶段 C · 工程化
  [x] 步骤 4   启动审计（治「插件没反应」）
  [x] 步骤 5   宿主事实作为服务（env）
  [x] 步骤 6   配置 schema
  [x] 步骤 7   日志
  [x] 步骤 8   热重载
阶段 D · 可成长
  [x] 步骤 9   patch 分层 + dump-config
  [x] 步骤 10  门禁脚本  ← 10 步全部完成
```

---

## 已确立的工程约束(不要破坏)

| 约束 | 落在哪 | 防什么 |
|---|---|---|
| **契约优先** —— 先改契约再改实现;删掉 `apps/api` 前端要能编译 | `contracts/README.md` | 前端依赖实现而非契约 |
| **只用可擦除语法** —— `erasableSyntaxOnly: true` | `tsconfig.base.json` | 被迫加构建步骤 |
| **`types` 按包声明** —— base 不写 `types` | 各包 tsconfig | 同构包偷偷绑死 Node |
| **按能力域分目录** —— `packages/<域>/<包>/` | 目录结构 | 改一个需求跳三个目录 |
| **配置属于 app** —— `cordis.yml` 在 `apps/cli/`,从包内路径解析 | `apps/cli/` | 仓库根目录暗示"只有一个应用" |
| **包名即分层** | 命名规范(见下) | 可替换性看不见 |
| **有 config 的插件必须导出 `Config` schema** | 各插件 src/index.ts | 配置写错时症状离根因三跳 |
| **可观测性设施由宿主挂载,不进配置树** | `apps/cli/src/boot.ts` | 漏掉启动最早期的日志 |
| **只有宿主能直接读 `process.env`** | `@ctl/env-launch` | 插件绑死 Node、依赖隐形 |
| **引导期和配置树共用同一份环境组装逻辑** | `loadLaunchEnv()` | 两边看到不一样的环境 |
| **清理只写 `ctx.effect`,不用 `apply` 的返回值** | 各插件 | 标准写法下 `apply` 的返回值**被静默丢弃** |
| **审计前先 `await ctx.loader.await()`** | `boot.ts` | `create()` 不等被唤醒的下游,审计会误判 |
| **基线声明全部候选,patch 只管开关** | `cordis.yml` + `patches/` | patch **改不了 `name`** |
| **本地只拦确定性的检查** | `.githooks/pre-commit` | 会误伤的门禁逼人 `--no-verify`,整套约束作废 |
| **契约包的 `import type {}` 不能删** | 各契约包 | 三家 lint 都误报;删掉 `tsc` 报 TS2664 |
| **版本号只写一处** —— 一律走 catalog 协议 | `pnpm-workspace.yaml` | 版本分叉 → 两份实体，静默翻车 |
| **框架本体不许开具名目录** | `verify-catalog` 的 `SINGLETON` | 两份 cordis 共存，`instanceof` 静默失效 |
| **宿主用 dependency，插件用 peer** | 各包 package.json | 框架被装成两份 |
| **`exports` 是库的属性，`bin` 是应用的属性** | 各包 package.json | app 和 package 形态混淆 |

### 包名规范

```
@ctl/cli                产品装配（apps/）
@ctl/<能力>             契约    ← 稳定，动它=动协议
@ctl/<能力>-<实现>      提供方  ← 随便换
@ctl/<能力>-<x>-policy  策略    ← 随便摘
@ctl/tool-<能力>        消费方  ← 不挂就没有
```

> 换项目时全局替换 `@ctl` 这个 scope 即可。

---

## 多语言规划(骨架已预留)

```
apps/cli/        TS   Cordis CLI            ← 当前在做
apps/api/        ?    Go / FastAPI / NestJS ← 预留，未定
apps/web/        TS   React                 ← 预留
apps/marketing/  TS   Next.js               ← 预留
gopkgs/          Go   共享库（go.work 管）   ← 预留
contracts/       —    跨语言契约（枢纽）      ← 预留 + 规矩已写
infra/           —    k8s / docker           ← 预留
```

### API 选型的关键结论

- **NestJS**:装饰器 + 参数属性 + 元数据 → **开发期必须构建**,且不能纯 ESM
  (Node 22 的 `require(esm)` 缓解了"用不了 ESM 库"的痛,但依赖链上有顶层 await 仍然不行)
- **Go / Python FastAPI / TS+Hono/Fastify**:开发期零构建
- **如果选 NestJS**:把它隔离成"例外",在 `apps/api/tsconfig.json` 里显式
  `erasableSyntaxOnly: false` + 注释说明为什么

---

## 已踩的坑(都记在根 README 的「已知陷阱」)

1. **`cordis.yml` 顶层必须是数组** —— 全部注释掉 = `null`,报
   `TypeError: config file must be a top-level array`;要空就写 `[]`
2. **条目 `id` 必须唯一** —— `duplicate loader entry id: xxx`
3. **读 Cordis 错误三步** —— 看动词(`import`/`apply`)→ 看括号里的 `id` → 看最底下 `[cause]`
4. **`--import tsx` 依赖 cwd** —— 仓库外调用会 `Cannot find package 'tsx'`;裸 node 无此问题
5. **`@types/node` 要显式装** —— tsconfig 写了 `types: ["node"]` 但没装包,
   报错离根因有两跳(「找不到 process」→ 其实是「@types/node 没装」)
6. **叶子包要声明自己的依赖** —— pnpm 严格 node_modules 不提升
7. **`node-addon-require-builtin` 是 loader 的必需拼图** —— 它是 loader 的
   **可选 peer**，缺了不报错只静默降级：裸包名会从 loader 自己所在目录解析，
   而不是从 `ctx.baseUrl`（配置文件所在目录）。症状是明明装好了却
   `Cannot find package '@ctl/xxx'`。装在 `apps/cli`（谁启动 loader 谁喂 peer）
8. **`packageManager` 由 corepack 强制执行** —— 写错版本号会触发
   `ERR_PNPM_ABORTED_REMOVE_MODULES_DIR`（要求清空 node_modules 重装）。
   本仓库基线 `pnpm@10.17.0`，升级必须是一次有意识的动作
10. **`PENDING` 不是阻塞** —— `inject` 依赖缺失时 `ctx.plugin()` **立刻返回**，
    fiber 停在 `PENDING`(=0)。它不占 event loop 任何资源,所以进程照常退出、
    退出码 0、什么都不打印。**这是「插件没反应」的成因**,不是 bug 是设计
    (依赖晚点到就会自动激活,实测 300ms 后挂上提供方 → 消费方自己变 ACTIVE)
11. **git hook 里必须用 `git diff --name-only -z`** —— 默认输出会把非 ASCII
    文件名转义成 `"docs/\346\236\204..."` 这种带引号的形式。
    **中文文件名必然踩到**:后面的 `[ -f "$f" ]` 判断失败,
    配合 `set -e` 直接杀掉整个 hook —— 而且是**静默的**:
    `git commit` 什么都不打印,也不生成提交,你以为自己提交成功了。
    实际排查花了三次尝试才定位。
9. **参数属性 `constructor(ctx, private config: C)` 不可擦除** —— 违反
   `erasableSyntaxOnly`，裸 node 报 `TypeScript parameter property is not
   supported in strip-only mode`。写成普通字段 + 普通参数。`pnpm typecheck` 能提前拦住

---

## 已验证的事实

- ✅ 裸 `node xxx.ts` 能跑(Node 22.23 type stripping 默认开启)
- ✅ 裸 node 能解析 **workspace 包的 `.ts` exports 入口**
- ✅ `enum` 在裸 node 下报 `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX`,`erasableSyntaxOnly` 会在编译期拦住
- ✅ 配置从**包内路径**解析后,在 `/tmp` 目录调用 CLI 也能正常工作
- ✅ 同一插件挂两次 → 打印两遍(Plugin 是品牌,Fiber 是门店)
- ✅ **peer 缺失时 pnpm 不报错也不警告,而是"替你挑一份"偷偷装进插件隔间** ——
  你顶层 import 不到它。所以 peer 不是强制,是尽力而为
- ✅ **peer 版本冲突同样静默** —— 宿主版本满足 peer 范围 → 共用一份;
  不满足 → 各用各的,**两份实体,零警告**(实测 `is-odd@2` vs peer `^3`)
- ✅ `catalog:` 在 pnpm 10.17 可用,lockfile 会生成顶层 `catalogs:` 段,是唯一真相源
- ✅ `pnpm -r run <script>` **不含根包**(需 `--include-workspace-root` 才含)
- ✅ `exports` 是白名单:没登记的路径一律 `ERR_PACKAGE_PATH_NOT_EXPORTED`,
  **包括 `package.json` 自己**,所以每个包都要写 `"./package.json": "./package.json"`
- ✅ harness 的 `apps/cli` **没有 `exports`,只有 `bin`**;238 个包 100% 有 tsconfig,
  0 个有 vitest 配置 —— 编译边界=包边界,测试是横切的

---

## 步骤 3 已完成:第一条能力 seam

### 成果

```
packages/logging/                           ← 能力域三：日志
└── logger-file/  @ctl/logger-file       exporter：JSON Lines 落盘（无契约包，契约是 Cordis 内置的）

packages/host/                              ← 能力域二：宿主事实
├── env/          @ctl/env               ① 契约（同构）
├── env-launch/   @ctl/env-launch        ② 提供方 A：shell + app .env 两层快照（带 types:node）
└── env-static/   @ctl/env-static        ② 提供方 B：配置里写死（同构）

packages/notify/                            ← 能力域一：通知
├── notify/          @ctl/notify           ① 契约（只有 interface + declare module）
├── notify-console/  @ctl/notify-console   ② 提供方 A：打控制台
├── notify-file/     @ctl/notify-file      ② 提供方 B：写文件（唯一带 types:node 的包）
└── tool-notify/     @ctl/tool-notify      ③ 消费方（inject: ['notify']）
```

### 换实现实验的结果

改 `apps/cli/cordis.yml` 里**一行 `name` + 一段 `config`**：

```yaml
- id: notify
  name: '@ctl/notify-file'      # 原来是 '@ctl/notify-console'
  config:
    path: './notify.log'        # 相对 cordis.yml 所在目录
```

```
# 换之前
📣 [ops] CLI 启动完成
   这条消息由 tool-notify 发出，它不知道是谁在负责发送。

# 换之后
📄 已写入 /…/apps/cli/notify.log
```

**`tool-notify` 和 `notify` 两个包一个字没改。** 消费方源码里不含 console / file / fs 任何一个词。

### 步骤 3.1:依赖体系整改(同批完成)

| 改动 | 为什么 |
|---|---|
| `pnpm-workspace.yaml` 加 `catalog:` | 版本号收口到一处,分叉不可能发生 |
| `pnpm-workspace.yaml` 加 `allowBuilds` | 消掉每次 install 的 esbuild 黄框 |
| 五个包版本改成 `catalog:` | 同上 |
| `packages/*` 的 cordis 改 **peer + dev** | 语义:插件用宿主那份。**注意这是文档,不是强制** |
| `apps/cli` 补 `@ctl/notify` | 宿主真的提供 peer,而不是靠各包 dev 兜 |
| 根加 `packageManager` / `engines` | corepack 会强制执行;裸 node 跑 TS 有 Node 版本门槛 |
| 每包加 `typecheck` script,根 `pnpm -r run typecheck` | **第一道门禁**,步骤 10 的第一块砖 |
| 每包加 `"./package.json"` 导出 + `description` | exports 是白名单;description 是 227 包规模下的救命稻草 |

> **门禁当场就见效了**:写 `notify-file` 时用了参数属性,`pnpm typecheck` 立刻报
> `TS1294 erasableSyntaxOnly`,不用等到运行时。

### 依赖三分法(结论)

判据不是"何时用",是 **"消费者装了我之后,跑我的代码时缺了它会不会炸"**。

```
消费者运行时不需要        → devDependencies（工具链 / 纯类型 / peer 的本地替身）
需要，且可以有两份        → dependencies
需要，且必须全场唯一      → peerDependencies + devDependencies
需要，但装不上也不该中断  → optionalDependencies
```

同一个 cordis：`apps/cli` 是 `dependency`（宿主，真的装），`packages/*` 是 `peer`（插件，用宿主那份）。

---

## 步骤 4 已完成:启动审计

### 解决什么

Cordis 头号坑「插件没反应」:`inject` 的服务永远不出现时,fiber 停在 `PENDING`,
**进程正常退出、退出码 0、零输出**。功能悄悄少一块,没人知道。

### 做法

新增 `apps/cli/src/audit.ts`,在 `loader.create()` **之后**巡场一遍(位置关键:
早于结算会把「还没轮到加载」误判成「永远等不到」)。

两关:

| 关 | 判据 | 说明 |
|---|---|---|
| 一 | `entry.fiber === undefined && !entry.disabled` | 模块压根没加载 |
| 二 | `fiber.state !== ACTIVE` | 加载了但没启动 |

第二关按状态分流:`FAILED` → `await fiber.await()` 取出原始错误;
`PENDING` → `Object.keys(fiber.inject)` 减去 `fiber.ctx.get(s) !== undefined`
= **它在等谁**。

`FiberState` 是 const enum,`erasableSyntaxOnly` 下不能当值用,
所以做值镜像 `const FIBER_ACTIVE = 2 as FiberState.ACTIVE`(harness 同款写法)。

### 故障归属边界(实测确认)

| 故障 | 谁来管 |
|---|---|
| 模块找不到(`import` 阶段) | **loader 自己**,当场炸 |
| `apply` 里抛错(`apply` 阶段) | **loader 自己**,当场炸,审计根本执行不到 |
| **依赖等不到(`PENDING`)** | **只有审计能抓** |

### 验收结果

```
实验 1 正常情况         → 不误报,三行输出,exit 0
实验 2 注释掉提供方     → Error: ctl: 1 个插件没能激活
                            @ctl/tool-notify
                              等待服务：notify
                          exit 1
实验 3 apply 抛错       → loader 先炸,审计未执行(边界符合预期)
```

---

## 步骤 5 已完成:宿主事实作为服务

### 解决什么

插件直接写 `process.env.XXX` 会同时坏掉三样东西:

1. **绑死 Node** —— 同构包再也跑不了浏览器 / Worker / Deno
2. **依赖隐形** —— `inject` 那行看不出它还要一个环境变量,生产上少配了才发现
3. **测不了、换不了** —— 想换测试值只能改全局;想换远程配置源只能改代码

解法和 notify 一模一样:**把「宿主事实」也做成一块招牌**。

### 抄自 harness `dsh-launch-environment` 的两个设计

**① 快照,不是实时读。** 构造时对 `process.env` 拍一次照,之后只看照片。
`process.env` 是可变全局,任何第三方库都能中途改它 —— 实时读意味着同一个插件
在不同时刻可能拿到不同的值,而且查不出是谁改的。

**② 值带来源,不只是字符串。**

```ts
export interface EnvValue { value: string; source: string }
getFrom(name: string, sources: string[]): EnvValue | undefined
```

harness 原话:**"Omitting a layer is a refusal, not a demotion."**
(不列出某一层是拒绝,不是降级)—— `getFrom('API_KEY', ['process'])` 是明确宣布
「只接受 shell 传进来的」,将来优先级怎么调整都漏不进来。
现在只有一层,但接口先按分层设计,步骤 9 的 patch 分层会直接用上。

### 验收结果

```
① 默认                              → 收件人 ops   来自 (默认值)
② CTL_NOTIFY_TO=alice               → 收件人 alice 来自 process
③ 换成 env-static + 仍带 alice      → 收件人 bob   来自 static   ← alice 被彻底无视
④ tool-notify 里写 process.cwd()    → TS2580 Cannot find name 'process'  ← 门禁挡住
```

③ 是核心:**环境变量设了却完全不起作用**,因为这次挂 env 招牌的店压根不读
`process.env`。而 `tool-notify` 一个字没改。

### 消费方的变化

```diff
-export const inject = ['notify']
+export const inject = ['notify', 'env']

-  await ctx.notify.send({ to: 'ops', … })
+  const found = ctx.env.get('CTL_NOTIFY_TO')
+  await ctx.notify.send({ to: found?.value ?? 'ops', … })
```

依赖从「藏在函数体里的 process.env」变成「写在 inject 那一行、一眼可见」。

---

## 步骤 6 已完成:配置 schema

### ⚠️ 更正一条旧结论

此前说过「Cordis 官方要求用 Schemastery,不要纠结」—— **在 cordis 4.0.1 上这是错的**。

```js
// cordis/lib/index.js resolveConfig
const result = runtime.Config["~standard"].validate(config)
```
```ts
// registry.d.ts
Config?: StandardSchemaV1<any, T>
```

`~standard` 是 **Standard Schema** 规范。**任何实现它的库都行** ——
zod 3.24+/4、valibot、arktype、schemastery。实测 schemastery 和 zod v4 均可用,
报错信息各有格式但都能被 cordis 正确渲染。

**模板选 schemastery 的真实理由是:它双向。** schema 不只能校验,还能反过来
渲染成配置表单 —— harness 的 Web UI 自动生成插件配置界面靠的就是这个。
zod 只能单向。**确定不做配置 UI 的项目,用 zod 完全没问题。**

### 没有 schema 的代价(实测)

cordis.yml 里把 `path` 打成 `paht`:

```
Error: ctl: 1 个插件没能激活
  @ctl/tool-notify
    fiber 状态异常：1
```

报的是 **tool-notify**,而改错的是 **notify-file** 的配置。断链:

```
paht → config.path = undefined
     → FileNotify 构造函数里 super(ctx,'notify') 先跑,招牌挂上了
     → 下一行 new URL(undefined, baseUrl) 抛错,招牌又被摘走
     → 这中间 tool-notify 已被唤醒、apply 跑到一半
     → 审计跑过来时它卡在 LOADING(1)
```

**根因和症状隔三跳,中间还夹一次「招牌挂上又被摘走」的竞态。**

### 有 schema 之后

```
failed to apply loader entry notify (@ctl/notify-file): invalid config:
  - $.path missing required value (at path)
```

哪个条目 / 哪个包 / 哪个字段 / 什么问题,一行说完。

**关键在时机:校验发生在 `apply` 之前。** 所以插件代码里可以直接写
`config.path`,不必做任何防御性判断 —— 拿到手时已被保证合法。

### 写法

```ts
export interface Config { path: string }

/** 名字必须叫 Config（Cordis 认这个名字），和 interface 同名做声明合并 */
export const Config: Schema<Config> = Schema.object({
  path: Schema.string().required().description('…'),
})
```

`interface Config` 在类型空间、`const Config` 在值空间,TS 声明合并允许同名。
这是 cordis 生态的标准写法。

**schemastery 进 `dependencies` 而非 peer** —— 它不做 `instanceof` 身份判断,
两份共存不会翻车。判据表第二问的直接应用。

### 验收结果

```
① paht 拼错        → $.path missing required value (at path)
② path: 123        → $.path expected string but got 123 (at path)
③ values 写成数组  → $.values expected object but got CTL_NOTIFY_TO=bob (at values)
④ 完全不写 config  → 正常跑，values 默认 {}，输出「收件人 ops 来自 (默认值)」
```

---

## 步骤 7 已完成:日志

### 意外一:契约是 Cordis 自带的

原计划建 `@ctl/logger` 契约包,扒源码发现不需要:

```ts
// cordis/lib/types/context.d.ts
logger: LoggerService
// cordis/lib/types/logger.d.ts
export interface Exporter { levels?: Record<string, number>; export(message: Message): void }
```

```
契约   →  Cordis 内置的 LoggerService + Exporter    ← 不用自己写
提供方 →  实现 Exporter 的插件(console / file / 远程)
消费方 →  各插件里的 ctx.logger.info(...)
```

**契约不一定要自己写,框架给了就直接用。**

### 意外二:sink 必须由宿主挂载

链条(全部实测):

1. `ctx.logger` 永远可用,但**没有 sink 时消息被静默丢弃**
2. 消息进了 `ctx.logger.buffer`(保留最近 1000 条),但**不会回放**给后来的 sink
3. 配置树条目是 `Promise.allSettled` **并发** apply 的 ——
   `cordis.yml` 里写在第一行也保证不了先跑完
4. 想 `inject: ['logger']` 等它?**永远 PENDING** ——
   logger 是 Context 内置属性,不在 registry 里:
   `ctx.get('logger')` 和 `ctx.get('logger', false)` 都返回 `undefined`

**证据**:文件 sink 挂在 cordis.yml 时,`ctl.log` 里最小的 `sn` 是 3;
而 `ctx.logger.buffer` 里明明有 `sn=1 name=hello`。挪进 `boot.ts` 后 `sn=1` 就有了。

所以:

```ts
await ctx.plugin(Loader)
await ctx.plugin(ConsoleExporter, { showTime: 'hh:mm:ss' })   // ★ 必须在 create 之前
const logFile = process.env.CTL_LOG_FILE                       // 宿主才能直接读 env
if (logFile) await ctx.plugin(FileExporter, { path: logFile, levels: {} })
await ctx.loader.create({ … })
```

> harness 的 `boot()` 为此留了 `prepare` 钩子:
> "run after Loader installation and **before any config-tree entry mounts**"。

### 三个细节

**① 级别数字是「啰嗦程度」不是「严重程度」**

```
ERROR=0（永远显示） INFO=1（默认阈值） WARN=2（默认看不见！） DEBUG=3
```

**② logger 名字自动取自插件 `name`**;但在 `Service` 内部是从**类名**推导的
(`class ConsoleNotify` → `console-notify`,和包名 `notify-console` 对不上),
所以显式写 `this.ctx.logger('notify-console')`。

**③ sink 可叠加** —— 每条消息送到所有已注册 sink。不是「换 exporter」是「再加一个」。
(`ctx.logger.exporters` 里本来就有一个内置的 buffer sink,所以 size 总是比你挂的多 1。)

### `@ctl/logger-file`

JSON Lines,每条同时存结构化 `args`(机器可读)和渲染后的 `msg`(人可读,
用 `Logger.format(this, message)` 解 printf 占位符)。

**用 `appendFileSync`** 是有意识的取舍:CLI 可能在异步写落盘前就退出。
长驻服务该换成带 flush 的异步队列,代价是要处理退出收尾。

### 自己踩的坑

编辑 `cordis.yml` 时留了一条活着的 `logger-file` 条目没删,导致 FileExporter
被挂载两次 —— 表现是**每条日志在文件里出现两次**,以及「没设环境变量却还是
生成了 ctl.log」。**两个 sink 指向同一个文件 = 每条写两遍。**
排查靠的是 `exporters.size` 和 `sn` 字段。

---

## 步骤 7.1:分层启动环境(补步骤 5 的坑)

### 起因

`boot.ts` 里出现了一行裸的 `process.env.CTL_LOG_FILE`,和「插件不许碰 process.env」
看着矛盾。实测确认配置树确实来不及:

```
【探针1】挂完 Loader 之后     : ctx.env = undefined   ← boot 要用的时刻
【探针2】loader.create() 之后 : ctx.env = object
```

**引导悖论**:引导阶段需要的东西,不能来自引导过程本身的产物。
(日志 sink 撞过一次,环境值是第二次。)

### 解法:一份组装逻辑,两个入口

`@ctl/env-process` 改名 `@ctl/env-launch`,同时导出:

```ts
export function loadLaunchEnv(options): Record<string, EnvValue>   // 纯函数,宿主引导期用
export class LaunchEnv implements EnvService                        // 包装成服务
export function apply(ctx)                                          // 插件,配置树用
```

宿主不再裸读 `process.env`,而是调用**和插件同一份**的组装逻辑:

```ts
const env = new LaunchEnv(loadLaunchEnv({ baseUrl: ctx.baseUrl }))
const logFile = env.getFrom('CTL_LOG_FILE', ['process'])?.value
```

**两边看到的环境永远是同一份事实,而 env 提供方仍然可以在 cordis.yml 里换。**

### 两层,信任度递减

| 层 | `source` | 是什么 |
|---|---|---|
| shell / CI / 容器传进来的 | `process` | 这次运行的明确意图 |
| `apps/cli/.env` | `app-env` | 这个 app 的默认值 |

`source` 和 `getFrom` 到这里才真正有用武之地 —— 一层的时候它们是空转的。

### `getFrom` 的实战价值(实测)

```
① 只有 .env               → 收件人 team-from-dotenv 来自 app-env
② shell 覆盖              → 收件人 alice           来自 process
③ 把 CTL_LOG_FILE 写进 .env → sneaky.log 【没有生成】
   从 shell 传             → ctl.log 生成了
```

③ 是关键:`getFrom('CTL_LOG_FILE', ['process'])` **省略 'app-env' 是拒绝,不是降级**。
运维开关不该被 app 自己提交进版本库的 `.env` 悄悄打开。

### 文件约定

- `apps/cli/.env.example` 进版本库(默认值,非密钥)
- `apps/cli/.env` 被 gitignore(`.env` / `.env.*`,但 `!.env.example`)
- 没有 `.env` 也能跑,回落到代码里的默认值

### 自己踩的坑

`constructor(private data: …)` 参数属性又违反 `erasableSyntaxOnly`,
`pnpm typecheck` 当场拦下(这已经是第二次,写类时要条件反射)。

---

## 步骤 8 已完成:热重载

### 三件事同时合上了

**① 谁让进程活着** —— HMR 自带 chokidar 文件监视器,那是个长期句柄。
实测:装上它 `timeout 3` 会超时退出(码 124),不装就 0.28s 自己退出。

**② 动态加载怎么发生** —— 实测两种:

```
改插件源码 apps/cli/src/plugins/hello.ts
  → hmr reload plugin at src/plugins/hello.ts
  → hello 插件启动了 ★改过了★

改 cordis.yml，把 notify-console 换成 notify-file
  → file-notify 已写入 .../notify.log      ← 提供方【活着换掉了】，消费方重新跑
```

**③ effect 的复原单第一次成为刚需** —— 见下面那个坑。

### ⚠️ 最重要的发现:`export function apply` 的返回值被静默丢弃

**初次归因错了**(以为是 HMR 不跑),真实变量是**插件的写法**。

cordis 调用插件前先判断(`lib/index.js:57`):

```js
function isConstructor(func) {
  if (!func.prototype) return false   // 箭头函数 / 对象简写方法：无 prototype
  return true                          // function 声明：有 prototype
}
```

然后分两条路(`:1066`):

```js
if (isConstructor(runtime.callback)) {
  const instance = new runtime.callback(this.ctx, this.config)
  return instance?.[symbols.init]?.()          // ← 返回值被丢弃
} else return runtime.callback(this.ctx, this.config)   // ← 返回值会被 collect(:1142)
```

**四种写法两种结果**(实测):

```
export function apply(c){}      prototype 存在 → 当构造函数 → 返回值丢弃 ✘
{ apply: function(c){} }        prototype 存在 → 同上                    ✘
{ apply(c){} }   简写方法        无 prototype   → 正常调用 → 收集         ✔
{ apply: (c)=>{} } 箭头函数      无 prototype   → 同上                    ✔
```

**cordis 生态的标准写法就是 `export function apply(ctx)`** ——
所以在我们所有插件里,`apply` 的返回值**永远不会成为复原单**,
不管走哪条卸载路径。不是「HMR 时不跑」,是「根本没被登记过」,
而且完全静默:无警告、无报错。

这就是 cordis 系列里那句「effect 是唯一的生命周期挂点」的**机制层面答案**:
不是建议,是标准写法下**没有第二个选择**。

顺带:`new apply(ctx, config)` 意味着 `apply` 是被 `new` 调用的,
函数体里 `this` 是个新对象而不是 `undefined`。插件里别用 `this`。

另外单独验过:`ctx.on('dispose', …)` 在任何路径下都**不触发**。

### 接线是宿主的活

两处都得宿主自己接:

**a. HMR 的 peer 服务** —— `Hmr.inject = ['loader', 'timer']`。
不挂 `cordis-plugin-timer`,HMR 就永远 PENDING(会被启动审计抓到)。

**b. cordis.yml 本身不在监视范围内** —— HMR 主监视器只认 `root` 下的**模块**,
YAML 不是模块。要用 `hmr.registerConfig(filename, refresh)` 单独挂精确路径。
而 `cordis-plugin-include` **不会自己接**(源码里没有任何 hmr 引用)。

Include 实例就是那条 entry 的 `subtree`,它有公开的 `refresh()`:
「重读文件,内容变了就事务性地刷新子条目」。

```ts
const includeId = await ctx.loader.create({ name: '…include', config: { path } })
for (const entry of ctx.loader.entries()) {
  if (entry.options.id !== includeId) continue
  const tree = entry.subtree as { refresh?: () => Promise<void> } | undefined
  if (typeof tree?.refresh === 'function') {
    await ctx.hmr.registerConfig(configPath, () => tree.refresh!())
  }
  break
}
```

### 为什么走显式开关而不是配置项

`CTL_WATCH=1` / `pnpm ctl:watch`。理由:**这是运行形态的切换,不是普通配置。**
HMR 一装上进程就不会自己退出了 —— 一次性 CLI 默认不该这样,
不然每敲一次命令都要 Ctrl+C。

三种形态,同一个内核:

```
一次性 CLI（默认）  boot → 干活 → event loop 空 → 退出 0
watch 模式          boot → HMR 持有 watcher → 一直活着
长驻服务（将来）    boot → 某插件 listen(3000) → 一直活着
```

### 遗留

**watch 模式下审计只覆盖首次结算。** 热重载后新出现的 PENDING 不会被抓 ——
`assertEntriesActivated` 是一次性的。要覆盖得监听 `internal/status` 事件,
留给以后。

---

## 步骤 9 已完成:patch 分层 + dump-config

### patch 的真实语义(全部实测)

| 字段 | 语义 |
|---|---|
| `id` | **定位**,必填 |
| `name` | **断言** —— 写了就必须和基线一致,不一致整条 patch 跳过并警告。**改不了 name** |
| `config` | **整体替换**,不是合并。`{a:1,b:2}` 打 `{b:99}` 的结果是 `{b:99}` |
| `disabled` | 开关。`false` 能把基线里关掉的重新打开 |
| `insert` | 往 **group** 条目里追加(目标不是 group 会警告跳过) |

- 多条 patch 依次执行,后面的能命中前面 `insert` 进来的条目
- 匹配不到只警告并跳过,**不让启动失败**
- 原始数据永不被修改,返回的是脱离的副本

### 由此推导出的组织方式

**`name` 改不了** → 「换实现」不能靠 patch 改 name,只能:

> **基线 `cordis.yml` 声明全部候选(备选项 `disabled: true`),
> patch 只负责开关。**

额外好处:一眼看得出这个 app 到底可能装哪些东西,而不是散在若干 patch 文件里等你拼。
而且**没人能靠 patch 偷偷塞进一个基线里没声明过的插件**。

### 文件布局

```
apps/cli/
├── cordis.yml            基线:全部候选
└── patches/
    ├── prod.yml          通知落盘（notify → notify-file）
    └── test.yml          环境值写死（env → env-static），输出可重复
```

### `--dump-config`

关键不在「能打印」,在**它和真正启动走同一个 `applyEntryPatches`**。
include 的官方注释:这个函数被 mounting 和离线 dump 共用,
「**所以 dump 永远不会和真正启动的东西脱节**」。

```bash
pnpm ctl --dump-config                      # 基线
CTL_PROFILE=prod pnpm ctl --dump-config     # 叠加后的最终组合
diff <(pnpm ctl:dump) <(CTL_PROFILE=prod pnpm ctl:dump)
```

解析要用 include 导出的 `entryListSchema`(js-yaml 必须 `^4.1.0` 同大版本),
否则 `!!js` 表达式节点解析不出来 —— 那个 tag 名就叫 `!!js`(`tag:yaml.org,2002:js`)。

### ⚠️ 顺带修掉一个真 bug:审计跑太早

切到 notify-file 之后,审计**确定性地**报
`@ctl/tool-notify fiber 状态异常：1`(LOADING),但配置完全正确。

量时序发现:

```
+0ms    notify=2  tool-notify=1     ← provider 已激活，consumer 还在 apply 中
+11ms   全部 =2
```

**`loader.create()` 只保证「这条 entry 挂上了」,不保证被它唤醒的下游也跑完了。**

修法:审计前先调 `EntryTree` 的公开方法 —— 语义是「等到这棵树没有任何
进行中的导入或生命周期任务」,而且 `entries()` 递归进子树:

```ts
await ctx.loader.await()
await assertEntriesActivated(ctx, 'ctl')
```

回归验证:审计仍能抓到真 PENDING(注释掉提供方 → `等待服务：notify`)。

> 这个 bug 从步骤 4 起就潜伏着,只在「provider 唤醒 consumer」这条路径上暴露。
> notify-console 恰好快到能赶上,notify-file 慢一点就现形了 ——
> **典型的时序竞态:不是「有没有」,是「够不够快」。**

### 验收结果

```
① 基线              → notify-console，收件人来自 app-env
② CTL_PROFILE=prod  → notify-file 落盘
③ CTL_PROFILE=test  → 收件人 fixed-in-config 来自 static（shell 传的 alice 被无视）
④ profile 拼错      → 「找不到 profile：nope（期望文件 …/patches/nope.yml）」
⑤ 审计回归          → 注释掉提供方仍报「等待服务：notify」
```

### 引导期事实,现在共三个

全部只认 shell / CI 层(`getFrom(key, ['process'])`),写进 `.env` 无效:

```
CTL_CONFIG    整份换掉配置文件
CTL_PROFILE   叠加 patches/<name>.yml     ← 本步新增
CTL_LOG_FILE  日志同时落盘
CTL_WATCH=1   热重载模式
```

理由同一条:**一个提交进版本库的文件,不该有能力改变整个应用的组合。**

---

## 步骤 10 已完成:门禁脚本

### 这是「三层执行力」的最后一层

```
约定  →  写在这份构建日志里的工程约束        没有牙齿
工具  →  tsc / erasableSyntaxOnly           只管语言层面
门禁  →  scripts/verify-*.ts                ⭐ 唯一能拦住「能跑但违反架构」的改动
```

### 六道门禁

| 门禁 | 守什么 | 拦住的真实事故 |
|---|---|---|
| `package-invariants` | 包的形态与它在依赖图里的位置一致 | app 长出 `exports`、能力包长出 `bin`；`exports` 漏 `"./package.json"` |
| `dependency-layering` | 宿主用 dependency，插件用 peer;peer 必须配一份 dev | 框架被装成两份,`instanceof` 静默翻车 |
| `catalog` | 外部依赖一律 `catalog:` | 版本分叉 —— **peer 管不了这件事**(实测:pnpm 一句警告都没有,直接另装一份) |
| `runtime-closure` | `cordis.yml` 与 `patches/` 引用的包宿主都已声明 | **第一天撞的那个坑**;harness 有同名门禁 |
| `env-access` | 只有宿主能直接读 `process.env` | 插件绑死 Node、依赖隐形 |
| `effect-cleanup` | `apply` 不返回复原单 | 返回值被**静默丢弃**,热重载下泄漏句柄 |

### 关键实现决定

**① 不读 `pnpm-workspace.yaml` 的 glob** —— 门禁检查的是**约定的目录结构**
(`apps/*` 和 `packages/<域>/*`)。配置被改坏了,门禁反而更该报警。

**② 一次报全部,不是修一个跑一次** —— `verify.ts` 跑完所有门禁再汇总退出。

**③ 报错必须带修复建议** —— 每条违规都有 `fix`。门禁的价值不在「拦住」,
在「拦住之后让人知道怎么办」。

**④ 注释要挖空再检查,且保留行号** —— `env-access` 第一次跑就误报了:
`tool-notify` 的一句注释里提到了 `process.env`。
用等长空白替换注释内容,行号才不会错位。

### ⭐ 绿灯本身不能说明什么

**一道从不失败的门禁等于没有。** 六道门禁全部做了下毒测试:

```
① 删掉包的 description          → ✅ 抓到
② exports 去掉 "./package.json" → ✅ 抓到
③ 能力包的 cordis 改成 dependency → ✅ 抓到
④ 写死一个版本号                 → ✅ 抓到
⑤ cordis.yml 引用未声明的包      → ✅ 抓到
⑥ patch 里引用未声明的包         → ✅ 抓到（且报错指向 patches/prod.yml 而非 cordis.yml）
⑦ 插件里偷读 process.env        → ✅ 抓到（精确到行号）
⑧ apply 里 return 一个函数       → ✅ 抓到
```

**新增门禁时必须配一个下毒测试** —— 否则你不知道它到底在不在干活。

### CI

`.github/workflows/ci.yml`:类型 → 门禁 → 冒烟(默认组合能起来 + 三个 profile 的
最终组合算得出来)。

不写死 pnpm 版本,靠 `corepack enable` 去读 `packageManager` 字段 ——
**那个字段是会被强制执行的,不是建议**(实测:写错版本会触发清空 node_modules)。

### 命令

```bash
pnpm verify              # 六道全跑
pnpm verify catalog      # 只跑一道（前缀匹配）
pnpm check               # typecheck + verify
```

---

## 补记:catalog 的两个易混点(实测)

### ① `catalog` 和 `catalogs` 分属两套语法

```
pnpm-workspace.yaml 里 = 【配置字段名】
  catalog:      装「默认目录」
  catalogs:     装「一堆具名目录」→ 所以是复数

package.json 里 = 【依赖协议】，和 workspace: / npm: / file: 同类
  "catalog:"        默认目录
  "catalog:cli"     名叫 cli 的目录      → 协议名永远单数
```

实测:写成 `"catalogs:cli"` 报
`ERR_PNPM_SPEC_NOT_SUPPORTED_BY_ANY_RESOLVER`。
**复数只出现在「装东西的容器」那一侧。**

另外具名目录必须是**独立顶层键**。写成这样会报
`Invalid catalog entry for cli. Expected string, but found: object`:

```yaml
catalog:
  is-odd: '3.0.1'
  cli:                    # ❌ 嵌在 catalog 里 → pnpm 把 cli 当成包名
    is-odd: '2.0.0'
```

（`"catalog: cli"` 带空格能用,pnpm 会 trim。但别指望这种宽容。）

### ② 「版本号只写一处」≠「全仓库只准一个版本」

这两件事我一开始在门禁里混了,导致 `catalog:legacy` 被误判成硬版本。

不同 app 对同一个库依赖不同大版本是**正常工程现实**(web 用 react 19、
marketing 的旧站停在 18)。具名目录就是为此而生 —— **版本号依然只写在
`pnpm-workspace.yaml` 里,只是分了组。**

门禁该拦的是「散落在各 package.json 里的硬版本」,不是「有几个版本」。

### ③ 但框架本体是例外

实测两份 cordis 共存(`4.0.1` vs `4.0.1-rc.1`):

```
❌ 两份 Service / Context 类不是同一个
❌ 服务实例在宿主眼里不是 Service（instanceof false）
❌ 用另一份 cordis 建的 ctx 拿不到已挂的服务（undefined）
✅ 但基本调用居然还能走通        ← 最危险的地方
```

**它不会当场崩。** cordis 用 `Symbol.for` 注册服务、`Service` 自定义了
`hasInstance`,所以大部分路径侥幸能跑。只要有一处代码依赖真正的类身份,
就会拿到 `false` 或 `undefined` —— **而且没有任何报错。**

比直接崩坏得多:直接崩当场就修了,现在是「跑得好好的,某个分支下悄悄不对」。

**已写进门禁**:`verify-catalog` 的 `SINGLETON` 名单里的包出现在具名目录里 → 报错。

### ④ `catalogMode` 能消掉维护成本

```yaml
catalogMode: strict
```

之后 `pnpm add X` 会**自动**把 `"catalog:"` 写进 package.json、把版本号写进
`pnpm-workspace.yaml`,一步都不用多走。

⚠️ 但装冲突版本时会报 `ERROR Invalid Version: ^3.0.1` —— 报错信息毫无帮助,
它想说的是「catalog 里已经有了,冲突」。这时候手工建具名目录即可。
(`strict` 和 `prefer` 两个模式都这样。我们暂时没启用,因为这个粗糙边缘
比自动入库的收益更容易让人困惑。)

---

## 补记:lint 选型(三家实测)

### 数据(23 个文件 / 1562 行,全是本仓库真实代码)

| | 无类型信息 | 带类型信息 | 格式化 | import 排序 |
|---|---|---|---|---|
| **oxlint** 1.79 | 0.05s | ❌ 不支持 | ❌ 要另配 Prettier | ❌ **规则不存在**,配置直接解析失败 |
| **Biome** 2.5 | 0.06s | **0.20s** | ✅ 内置 | ✅ 内置 |
| **ESLint** 10.9 | 0.42s | **0.68s** | ❌ 要另配 Prettier | ⚠️ **插件在 ESLint 10 上崩** |

### 选 Biome 的三个理由(都不是速度)

**① ESLint 的插件生态真的会断。**
按常见配置写 `import/order`,跑起来直接崩在
`eslint-plugin-import/lib/rules/order.js:663` —— 它还没跟上 ESLint 10。
这类断裂每个大版本都来一次,对「未来可成长」的模板是长期维护税。

**② oxlint 会逼你改掉 cordis 的标准写法。**
它默认带 unicorn 规则集,把 `import type {} from '...'` 报成
「Empty import specifier is not allowed」—— 而那个空花括号是模块增强的必需前提。
另外它没有 import/order、不支持类型感知,两块都得再补别的工具。

**③ Biome 一个工具顶三个,抓到的问题质量最高。**
在本仓库真实代码里找到:未使用导入 ×3、非空断言 ×1、
`noConfusingVoidType`(契约里写的 `boolean | void`,只有它指出来)。
类型感知规则 `noFloatingPromises`(nursery)实测能抓悬空 Promise ——
对我们尤其重要:`ctx.plugin()` 返回 PromiseLike、`apply` 可以是 async,
漏一个 `await` 的后果步骤 9 已经见过了。

### ⚠️ 但 lint 会误报,而且我被它坑了一次

三家**全部**把契约包里的 `import type { Context }` 报成未使用的导入。
我照着删了,`tsc` 立刻:

```
error TS2664: Invalid module name in augmentation,
              module '@deepseek-ai/cordis' cannot be found
```

那行的作用不是「用这个类型」,是**让 TypeScript 知道 `declare module`
增强的是哪个模块** —— 静态分析看不出这层关系。

改成更准确的 `import type {} from '@deepseek-ai/cordis'` 并加注释说明。
**代码由人控制,lint 只是提意见。**

### 由此定下的门禁分工

```
提交前（.githooks/pre-commit）
  只跑格式化（--linter-enabled=false），自动修 + 重新暂存
  确定性、不改语义、不需要判断 → 拦得起
  修不好（语法错误）才拦

CI
  pnpm lint       会误报 → 让人看到，但不阻断心流
  pnpm typecheck  ⭐ 唯一的类型裁判
  pnpm verify     架构门禁
  冒烟            默认组合 + 三个 profile
```

**拦一次误报,就会有人加 `--no-verify`;加成习惯之后整套约束全废,
而你还以为它在保护你。**

### 顺带省掉的两个依赖

- **husky** → `prepare` 脚本一行 `git config core.hooksPath .githooks`
- **lint-staged** → Biome 自带 `--staged`

### 三个场景实测

```
格式乱      → 自动修好并放行    export const   x=1  →  export const x = 1
lint 有问题 → 放行（不误伤）
语法错误    → 拦下（格式化修不了）
```

---

## 补记:补上 effect 与 isolate 的活例子

### 起因:扫了一遍发现两个真缺口

```
ctx.effect     模板里 ❌ 零处      ← 05 篇是最长的一篇（1441 行）
isolate        模板里 ❌ 零处      ← 10 篇是第二长（923 行）
intercept      模板里 ❌ 零处
```

**两篇最厚的文档,模板里一个例子都没有。**

不是疏忽,是形态决定的:前面所有插件都是**无状态**的(挂招牌、发日志、写文件),
没有需要长期持有的句柄;只有一个 CLI,没有「同名不同实体」的需求。
**模板还没长到需要它们的规模。**

### 解法:一个定时任务包,同时用上两个

`@ctl/task-heartbeat` —— 按间隔发通知,跑满次数自己收摊。

**① `ctx.effect` 持有真实句柄**

```ts
const stop = ctx.effect(() => {
  const timer = setInterval(() => { … ; if (够了) stop() }, config.intervalMs)
  return () => { clearInterval(timer); log.info('复原单执行了 —— interval 已清除') }
})
```

关键点:**`ctx.effect` 的返回值是一张可以主动调用的复原单**(实测确认)。
所以任务能自己收摊,而不是把进程钉死 —— 一次性 CLI 的形态得以保留。

**② `isolate` 让同名招牌各自独立**

```yaml
- id: tenant-a
  name: '@deepseek-ai/cordis-plugin-group'
  isolate:
    notify: 'tenant-a'                 # ← 本区自成一个 notify 分区
  config:
    - id: a-notify
      name: '@ctl/notify-file'
      config: { path: './tenant-a.log' }
    - id: a-task
      name: '@ctl/task-heartbeat'
      config: { label: 'A 区', times: 2 }
```

`tenant-b` 同构。**消费方 `task-heartbeat` 里没有任何一处提到分区** ——
它只写 `inject: ['notify']`,拿到哪一块由它所在的区决定。

### 实测对照:isolate 是不是必需的

```
有 isolate：
  tenant-a.log   [A 区] A 区 #1 / #2
  tenant-b.log   [B 区] B 区 #1 / #2        ← 两块同名招牌，各自独立

去掉 isolate（其余一字不改）：
  [E] file-notify Error: service "notify" has been registered at <ConsoleNotify>
                                            ↑ 07 篇「规矩一」：招牌不能重名
```

### 顺带发现:事件不受 isolate 影响

`notify-audit` 挂在根上,它**同时收到了两个租户的 `notify/sent`**:

```
notify-audit 第 2 条已投递 → B 区
notify-audit 第 3 条已投递 → A 区
```

**服务被分区隔离了,事件没有。** 这是合理的 —— 审计本来就该看全局。
但如果你想要「按租户隔离的事件」,得自己在事件负载里带上租户标识。

---

## 补记:给门禁本身加测试

### 为什么门禁必须有测试

**一道从不失败的门禁等于没有。** 而门禁失效是**静默**的 ——
它照样输出 ✅,你照样以为它在保护你,直到某天一个本该被拦下的改动进了主干。

之前六道门禁是**手工下毒**验的。手工验证不可持续:改一行门禁代码,
谁保证它还抓得到?这是模板里唯一「没有安全网的安全网」。

### 配置:全仓库只有一份 vitest.config.ts

对照 harness 实测:**238 个包,238 份 tsconfig,0 份 vitest 配置。**

```
tsconfig  每包一份 —— 编译单元 = 包边界 = 发布边界，每个包必须能脱离仓库单独编译
vitest    只要一份 —— 测试是【横切】的：一次跑遍所有包，共用同一套 setup
```

测试文件放在被测对象旁边(`scripts/verify.spec.ts`),不集中到 `tests/` 目录 ——
改代码时测试就在眼前,不容易忘。

### 假仓库在临时目录里现造

不用固定的 `fixtures/` 目录,理由很实际:那些目录本身会被**真实门禁**扫到
(它们也在 `packages/` 底下),互相干扰。

```ts
export function makeRepo(files: Files): { root: string; cleanup: () => void }
export function validManifest(name: string, kind: 'app' | 'package'): Record<string, unknown>
```

每个用例在合规基线上**只改坏一处**,断言那一处被抓到。

### 29 个用例，三组

```
① 合规的仓库必须全部放行        6 个（否则门禁会逼人绕过去）
② 每道门禁都要真的抓得到        17 个（含报错指向的文件是否正确）
③ 本仓库回归                    6 个（这个仓库自己必须全绿）
```

第 ③ 组容易被当成多余,其实很关键 —— 见下面那次注入实验。

### ⭐ 元验证:这些测试真的会失败吗

同样的道理,一个从不失败的测试也等于没有。故意弄坏两道门禁:

```
弄坏 package-invariants（不再检查 description）  →  1 failed | 28 passed
弄坏 env-access（不再挖空注释）                   →  2 failed | 27 passed
                                                     ↑ 连「本仓库回归」都挂了
还原                                              →  29 passed
```

**第二次特别说明问题**:弄坏注释剥离之后,连这个仓库自己都过不了 ——
因为 `tool-notify` 的注释里确实写着 `process.env`。
**这就是第 ③ 组存在的理由:真实代码里的边角情况,比构造的用例更刁钻。**

### 加新门禁的规矩

```
新增一道门禁 → 同时加两个用例：合规放行 + 违规抓到（含报错文件正确）
```

否则你不知道它到底在不在干活。

---

## 补记:第五条 seam —— prompt(槽位语义不同)

### 起因:agent 类 CLI 的 prompt 需要多语言

而 prompt 多语言**不是 `t(key)` 那一套**。三件事粒度完全不同:

```
① 整份 prompt 换      →  换 section（seam）        人格、领域指令
② 局部行为指令        →  一句话写死在身份段         「用用户的语言回答」
③ 界面文案            →  t(key)                    日志、错误提示、--help
```

只有 ① 是新形状,值得做。

### ⭐ 它和前面四条 seam 的差别:槽位语义

```
notify seam   槽位 = 服务名。同名 → 【撞车报错】
              因为「谁来发通知」全场只能有一个答案

prompt seam   槽位 = section 名。同名 → 【替换】
              因为「人格是什么」应该允许被更专门的东西顶掉
```

抄的是 harness 的 system-prompt,它的注释说得最清楚:

> "an agent preset **shadows** the deployment's persona with its own —
> and **both sides naming the same section is what makes the replacement
> work rather than duplicate**"

### 第五种角色:贡献方

前面四种是 契约 / 提供方 / 消费方 / 观察方。贡献方的特别之处:
**它既 inject 一个服务,又往那个服务里放东西。**

```
packages/prompt/
├── prompt/                @ctl/prompt                 契约
├── prompt-registry/       @ctl/prompt-registry        提供方：注册表
├── prompt-persona-zh/     @ctl/prompt-persona-zh      贡献方：中文人格
├── prompt-persona-en/     @ctl/prompt-persona-en      贡献方：英文人格（同槽位）
├── prompt-runtime-facts/  @ctl/prompt-runtime-facts   贡献方：动态 section
└── tool-prompt/           @ctl/tool-prompt            消费方：组装并打印
```

### 关键机制:服务方法里的 `this.ctx` 是【调用方的】

这决定了注册能不能随贡献方一起卸载。实测:

```
服务方法里 this.ctx.fiber.name = contributor      ← 不是注册表自己
贡献方 A 注册了
── 卸载贡献方 ──
贡献方 A 被撤销了
```

原理是 `Service` 基类装的 tracker(07 篇「包装里装的是我是谁」):
别人通过 `ctx.prompt` 拿到的是一个代理,代理的 `.ctx` 指向**他自己**。

所以注册表里这样写就够了,**不需要任何反注册代码**:

```ts
section(section: PromptSection): void {
  this.ctx.effect(() => {                    // ← 记在贡献方的账上
    const previous = this.sections.get(section.name)
    this.sections.set(section.name, section)
    return () => {
      // 只有当前占位的还是我才撤销，否则会误删顶替我的那个人
      if (this.sections.get(section.name) !== section) return
      if (previous) this.sections.set(section.name, previous)
      else this.sections.delete(section.name)
    }
  })
}
```

### 实测

```
只挂 zh      共 2 段：runtime-facts(-50) → persona(0)   你是一个命令行助手。
只挂 en      共 2 段                                    You are a command-line assistant.
两个都挂     共 2 段  ← 仍然只有 2 段，不撞车，后者顶掉前者
```

### 顺带:中英不是互相翻译

```
英文 "Be concise." 很有效
中文直译「要简洁。」效果差很多，得写成
  「回答控制在三句话以内，不要复述用户的问题。」
```

**模型对不同语言的指令遵循度不一样。** 所以两份人格是
**各自调优的正本**,不是一份翻译另一份 —— 这正是 harness 那句
"Both languages carry equal authority" 的含义。

### 顺带二:模板第一次有了单元测试的范例

`prompt-registry/src/index.spec.ts` 六个用例,演示**怎么测一个 Cordis 服务**:

```ts
const ctx = new Context()
await ctx.plugin({ name: 'prompt-registry', apply })
```

**不需要 loader、不需要 cordis.yml、不需要任何配置文件。**
`new Context()` + `ctx.plugin()` 就是一个完整可用的容器 ——
这也是「Cordis 是运行时的编排,不是构建时的框架」的一个侧面。

其中一个用例特别值:**顶替者被卸载后,被顶掉的那个会回来**。

---

### 后续方向(不再是线性步骤)

- ~~**事件**~~ ✅ 已补(bail 一票否决 + emit 广播,见 `packages/notify/notify-audit`
  与 `notify-quiet-policy`)
- ~~**effect / isolate**~~ ✅ 已补(见下)
- ~~**测试**~~ ✅ 已做(见下)
- **构建**:步骤 7 记的账 —— `exports` 加 `types`/`default` 条件分支、tsconfig `references`
- **多语言**:`apps/api`(Go / FastAPI / NestJS)、`apps/web`、`contracts/`
- **回写文档**:见 [[docs-sync-practice]] —— 每推进一步就回写 cordis-study

---

## 十步走完了

```
阶段 A · 地基     0 骨架 / 1 第一个 bin
阶段 B · 接内核   2 最小 boot / 2.1 配置归属 / 2.2 裸 node
阶段 C · 工程化   3 能力 seam + 依赖体系 / 4 启动审计 / 5 宿主事实
                  6 配置 schema / 7 日志 + 7.1 分层环境 / 8 热重载
阶段 D · 可成长   9 patch 分层 + dump-config / 10 门禁
```

**现在的模板**:10 个 workspace 成员、三条能力域、六道门禁、四个引导期开关。

