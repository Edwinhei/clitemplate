# apps/cli/.env.example
#
# 复制成 .env 使用：cp apps/cli/.env.example apps/cli/.env
# .env 本身不进版本库（见根 .gitignore）。
#
# 这一层的信任度【低于】shell 传进来的环境变量 —— 同名时 shell 赢。
# 所以这里只放「这个 app 的默认值」，不要放密钥。
#
# 而且运维开关（比如 CTL_LOG_FILE）在这里写了也【不生效】：
# boot.ts 用 getFrom('CTL_LOG_FILE', ['process']) 明确只接受 shell 层。

CTL_NOTIFY_TO=team-from-dotenv
