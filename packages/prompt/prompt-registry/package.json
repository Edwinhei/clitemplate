{
  "name": "@ctl/prompt-registry",
  "description": "prompt 提供方：按 order 拼接、同名替换的注册表。注册随贡献方一起卸载",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@ctl/prompt": "workspace:*",
    "@deepseek-ai/cordis": "catalog:"
  },
  "devDependencies": {
    "@ctl/prompt": "workspace:*",
    "@deepseek-ai/cordis": "catalog:"
  }
}
